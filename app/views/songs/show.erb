<h2> Song <%= @song.name %> Details </h2>
<h3> Artist Name: <a href="/artists/<%= @artist.slug %>"><%= @artist.name %></a></h3>
<h3> Genres: </h3>

<% @song.genres.each do |genre| %>
<a href="/genres/<%=genre.slug%>"><%= genre.name %></a>
<% end %>

<% if flash.has?(:message) %>
  <%= flash[:message] %>
<% end %>
