<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Songs Index Page</title>
  </head>
  <body>
    <h1>List of Songs: </h1>
      <ul>
        <% @songs.each do |song| %>
        <li><a href="/songs/<%= song.slug %>"><%= song.name %></a></li>
        <% end %>
      </ul>
  </body>
</html>
